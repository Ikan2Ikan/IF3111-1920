function validateForm() {

    var l = document.forms['form']['laporan'].value.length;
    var a = document.forms['form']['aspek'].value;
    // var f = document.forms['form']['file'].value.exec;
    // var ekstensiOk = /(\.doc|\.docx|\.xls|\.xlsx\.ppt|\.pptx|\.pdf)$/i;
    var min = 20;

    if (l < min || l == ""){
        alert("Panjang Laporan Minimal 20 Karater!");
      return false;
    }
    if (a == "Pilih Aspek Pelaporan/Komentar:") {
     alert("Aspek harus dipilih");
     return false;
    }  
  //   if(f != ekstensiOk.exec(pathFile)){
  //     alert('File harus di upload!');
  //     return false;
  // }
}  

function validasiFile(){
  var inputFile = document.getElementById('file');
  var pathFile = inputFile.value;
  var ekstensiOk = /(\.doc|\.docx|\.xls|\.xlsx\.ppt|\.pptx|\.pdf)$/i;
  if(!ekstensiOk.exec(pathFile)){
      alert('Silakan upload file !');
      inputFile.value = '';
      return false;
  }
  {
    //Pratinjau gambar
    if (inputFile.files && inputFile.files[0]) {
        var reader = new FileReader();
        reader.onload = function(e) {
            document.getElementById('pratinjauGambar').innerHTML = '<img src="'+e.target.result+'"/>';
        };
        reader.readAsDataURL(inputFile.files[0]);
    }
}
}